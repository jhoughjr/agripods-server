<%= form_for(@heat_index) do |f| %>
  <% if @heat_index.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@heat_index.errors.count, "error") %> prohibited this heat_index from being saved:</h2>

      <ul>
      <% @heat_index.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
