<%= form_for(@smoke_level) do |f| %>
  <% if @smoke_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@smoke_level.errors.count, "error") %> prohibited this smoke_level from being saved:</h2>

      <ul>
      <% @smoke_level.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sensorID %><br>
    <%= f.text_field :sensorID %>
  </div>
  <div class="field">
    <%= f.label :value %><br>
    <%= f.text_field :value %>
  </div>
  <div class="field">
    <%= f.label :measuredAt %><br>
    <%= f.datetime_select :measuredAt %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
